% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barcoding_assembly.R
\name{assemble_barcoding_dataset}
\alias{assemble_barcoding_dataset}
\title{Assemble the Reference Library of the Molecular Diagnostic Branch}
\usage{
assemble_barcoding_dataset(
  wd_path,
  output_dir = "11_barcoding",
  target_genes_file = NULL,
  genes_map_file = NULL,
  manual_exclusions_file = NULL,
  apply_manual_exclusions = TRUE,
  checklist_path = NULL,
  min_species = 50,
  preferred_parent = "3593",
  ncbi_dr = NULL,
  force_download = FALSE,
  phylogeny_map_file = NULL
)
}
\arguments{
\item{wd_path}{Character. The \code{phylotaR} workspace, shared with the phylogeny (e.g.
\verb{0_phylotaR_raw_Ingroup}).}

\item{output_dir}{Character. Root of the branch. Refused if it lies inside a directory of the
phylogeny. Defaults to \code{"11_barcoding"}.}

\item{target_genes_file, genes_map_file}{Character. Marker definitions; default to the files
distributed with the package, as in \code{\link[=assemble_ingroup_phylotar]{assemble_ingroup_phylotar()}}.}

\item{manual_exclusions_file}{Character. Curated accession exclusions; defaults to
\code{inst/extdata/manual_exclusions_ingroup.csv}.}

\item{apply_manual_exclusions}{Logical. Apply the curated exclusions? Defaults to \code{TRUE}.}

\item{checklist_path}{Character. Accepted checklist (Korotkova \emph{et al}. 2021); defaults to the
checklist distributed with the package.}

\item{min_species}{Integer. A cluster is retained when it holds more than this number of species.
Defaults to \code{50}, as in the phylogeny.}

\item{preferred_parent}{Character. NCBI Taxonomy ID of the focal ingroup. Defaults to \code{"3593"}.}

\item{ncbi_dr}{Character or \code{NULL}. Directory of the BLAST+ binaries, used only when the workspace
has to be created.}

\item{force_download}{Logical. Mine GenBank again even if the workspace exists. Defaults to \code{FALSE}.}

\item{phylogeny_map_file}{Character or \code{NULL}. Cluster-to-locus table of the phylogeny, compared
with the branch's own when it exists. Defaults to
\verb{1_phylotaR_out_Ingroup/TABLE_CLUSTER_MARKER_ASSIGNMENT_INGROUP.csv} next to \code{wd_path}.}
}
\value{
Invisibly, a list with \code{registry}, \code{summary}, \code{marker_map}, \code{comparison} and \code{counts}.
Files written under \verb{output_dir/1_assembly/}: one FASTA per locus,
\code{TABLE_barcoding_accession_registry.csv}, \code{TABLE_barcoding_cluster_marker_assignment.csv},
\code{TABLE_barcoding_marker_assignment_comparison.csv} (when the phylogeny's table exists),
\code{TABLE_barcoding_discarded_names.csv} and \code{TABLE_barcoding_replication_summary_assembly.csv}.
}
\description{
Builds the multi-accession reference library of the molecular diagnostic section from the
\code{phylotaR} workspace shared with the phylogeny. The workspace is read, or created with the same
\code{phylotaR} call as \code{\link[=assemble_ingroup_phylotar]{assemble_ingroup_phylotar()}} when it does not exist, so either branch can run
first. Clusters are selected with the phylogeny's criterion (more than \code{min_species} species).
Sequences present in several clusters are resolved with the phylogeny's rule and the curated
accession exclusions are applied, but none of the reductions to one sequence per species is: every
accession of every species is kept. Each cluster is assigned to a locus with the same code as the
phylogeny, from GenBank metadata cached under \verb{output_dir/cache/}. Names are reconciled against the
checklist with the rule of \code{\link[=clean_taxonomic_names]{clean_taxonomic_names()}}; names that do not match are dropped and
reported. Synonyms are not resolved.
}
\examples{
\dontrun{
assemble_barcoding_dataset(
  wd_path = "0_phylotaR_raw_Ingroup",
  output_dir = "11_barcoding"
)
}
}
\references{
Korotkova, N., Aquino, D., Arias, S., Eggli, U., Franck, A., Gómez-Hinostrosa, C., Guerrero, P. C.,
Hernández, H. M., Kohlbecker, A., Köhler, M., Luther, K., Majure, L. C., Müller, A., Metzing, D.,
Nyffeler, R., Sánchez, D., Schlumpberger, B. O., & Berendsohn, W. G. (2021). Cactaceae at
Caryophyllales.org - a dynamic online species-level taxonomic backbone for the family.
\emph{Willdenowia}, 51(2), 251-270. \doi{10.3372/wi.51.51208}
}
