% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support-audit.R
\name{audit_calibration_support}
\alias{audit_calibration_support}
\title{Audit the Nodes a Calibration Table Addresses}
\usage{
audit_calibration_support(
  calibrations,
  constraints,
  tree,
  constraint_tree,
  support_trees = list(),
  active_only = TRUE
)
}
\arguments{
\item{calibrations}{A \code{data.frame} in the format of \code{inst/extdata/calibrations_bounds.csv}, with
columns \code{mrca}, \code{column}, \code{value}, \code{min}, \code{max} and \code{used_in_analysis}.}

\item{constraints}{A \code{data.frame} in the format of \code{inst/extdata/cactus_constraints.csv}, used to
translate a taxon-set name into terminals.}

\item{tree}{An \code{ape} \code{phylo} object, or a path to a Newick file, holding the reference topology.}

\item{constraint_tree}{An \code{ape} \code{phylo} object, or a path to a Newick file, holding the topology
passed to \code{RAxML-NG --tree-constraint}.}

\item{support_trees}{Named list of annotated support trees (\code{phylo} objects or paths), one per
metric, for example \code{list(fbp = "cactus_support_fbp.raxml.support", tbe = "...")}. One column is
added per element. Defaults to an empty list.}

\item{active_only}{Logical. Restrict to rows with \code{used_in_analysis == TRUE}. Defaults to \code{TRUE}.}
}
\value{
A \code{data.frame} with one row per calibration: \code{mrca}, \code{n_tips}, \code{node}, \code{constrained},
\code{is_root}, one support column per entry of \code{support_trees}, and \code{min} and \code{max}.
}
\description{
For every row of a calibration table, resolves the node its terminals address in the reference
topology and reports whether that node was imposed by the topological constraint and what support
it carries. This is the check that has to pass before a chronogram is interpreted: a bound placed
on a constrained node dates a node the analysis assumed, and a bound placed on a weakly supported
node propagates that uncertainty into every age downstream of it without recording that it did.
}
\examples{
\dontrun{
audit_calibration_support(
  calibrations    = read.csv(system.file("extdata", "calibrations_bounds.csv",
                                         package = "PhyloCactus")),
  constraints     = read.csv(system.file("extdata", "cactus_constraints.csv",
                                         package = "PhyloCactus")),
  tree            = "7_Phylogenetics/ml_search/cactus_search.raxml.bestTree",
  constraint_tree = "7_Phylogenetics/cactus_constraints.tree",
  support_trees   = list(fbp = "7_Phylogenetics/cactus_support_fbp.raxml.support",
                         tbe = "7_Phylogenetics/cactus_support_tbe.raxml.support")
)
}
}
