% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{calculate_ml_tree}
\alias{calculate_ml_tree}
\title{Infer Maximum-Likelihood Phylogeny under Constrained Search}
\usage{
calculate_ml_tree(
  raxml_bin_path,
  aln_file,
  part_file,
  constraint_file,
  outgroup = NULL,
  n_init_trees = "rand{25},pars{25}",
  seed = NULL,
  n_workers = NULL,
  min_threads_per_worker = 4L,
  threads = 4,
  output_dir = dirname(aln_file),
  prefix = "cactus_search"
)
}
\arguments{
\item{raxml_bin_path}{Character. System command or full path to executable \code{RAxML-NG} binary.}

\item{aln_file}{Character. Path to input PHYLIP supermatrix alignment file.}

\item{part_file}{Character. Path to partition file specifying substitution models per partition.}

\item{constraint_file}{Character. Path to Newick topological constraint scaffold file.}

\item{outgroup}{Character vector of terminals passed to \code{RAxML-NG --outgroup}, or \code{NULL} (default); multiple terminals are joined with commas. \code{RAxML-NG} writes an unrooted topology with these terminals placed first, so this argument orders the output rather than rooting the tree: the root is imposed downstream by \code{automate_treePL()} via \code{ape::root(..., resolve.root = TRUE)}. Declaring the same set at every stage keeps the output ordering consistent across the maximum-likelihood search, the bootstrap replicates and the temporal bootstraps. Derive it with \code{resolve_rooting_outgroup()} rather than naming a terminal by hand.}

\item{n_init_trees}{Character. Initial starting tree specifications. Defaults to \verb{"rand{25},pars{25}"} (25 random + 25 parsimony trees).}

\item{seed}{Integer. Random seed for reproducible tree search initialization. Defaults to \code{NULL} (random).}

\item{n_workers}{Integer. Parallel worker process count for RAxML-NG. If \code{NULL} (default), derived
from \code{threads}, \code{n_init_trees} and \code{min_threads_per_worker} so that workers divide the starting
tree count evenly and no worker idles in the final round. Set to \code{1} to reproduce a strictly
sequential search.}

\item{min_threads_per_worker}{Integer. Lower bound on threads per worker when \code{n_workers} is derived. Defaults to \code{4L}.}

\item{threads}{Integer. Total number of CPU threads available to the search. Defaults to \code{4}.}

\item{output_dir}{Character. Directory path to save resulting maximum-likelihood tree files. Defaults to \code{dirname(aln_file)}.}

\item{prefix}{Character. Output file prefix. Defaults to \code{"cactus_search"}.}
}
\value{
A named list containing paths to the best ML tree (\code{.raxml.bestTree}) and all evaluated trees (\code{.raxml.mlTrees}).
}
\description{
Infers the maximum-likelihood evolutionary hypothesis explaining the concatenated supermatrix under specified partition models
and topological constraint scaffolds using \code{RAxML-NG} (Kozlov \emph{et al.}, 2019).
Executes multiple independent tree searches starting from randomized and parsimony starting trees to avoid local likelihood Optima.
}
\examples{
\dontrun{
calculate_ml_tree(
  raxml_bin_path = "raxml-ng",
  aln_file = "ALIGNMENT_supermatrix.phy",
  part_file = "MODELTEST_cactus_phylo.part.aicc",
  constraint_file = "cactus_constraints.tree"
)
}
}
\references{
Kozlov, A. M., Darriba, D., Flouri, T., Morel, B., & Stamatakis, A. (2019). RAxML-NG: a fast, scalable and
user-friendly tool for maximum likelihood phylogenetic inference. \emph{Bioinformatics}, 35(21), 4453-4455.
\doi{10.1093/bioinformatics/btz305}
}
