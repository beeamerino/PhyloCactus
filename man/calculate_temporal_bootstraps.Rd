% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{calculate_temporal_bootstraps}
\alias{calculate_temporal_bootstraps}
\title{Estimate Temporal Bootstrap Replicates Constrained to Best ML Topology}
\usage{
calculate_temporal_bootstraps(
  raxml_bin_path,
  aln_file,
  part_file,
  best_tree_file,
  bs_trees = 500,
  outgroup = NULL,
  seed = NULL,
  threads = 4,
  workers = NULL,
  blopt = "nr_safe",
  output_dir = dirname(aln_file),
  prefix = "cactus_temporal"
)
}
\arguments{
\item{raxml_bin_path}{Character. System command or full path to executable \code{RAxML-NG} binary.}

\item{aln_file}{Character. Path to input PHYLIP supermatrix alignment file.}

\item{part_file}{Character. Path to partition file specifying substitution models.}

\item{best_tree_file}{Character. Path to reference maximum-likelihood tree topology file (used as constraint).}

\item{bs_trees}{Integer. Total number of temporal bootstrap trees to generate. Defaults to \code{500}.}

\item{outgroup}{Character vector of terminals passed to \code{RAxML-NG --outgroup}, or \code{NULL} (default); multiple terminals are joined with commas. \code{RAxML-NG} writes an unrooted topology with these terminals placed first, so this argument orders the output rather than rooting the tree: the root is imposed downstream by \code{automate_treePL()} via \code{ape::root(..., resolve.root = TRUE)}. Declaring the same set at every stage keeps the output ordering consistent across the maximum-likelihood search, the bootstrap replicates and the temporal bootstraps. Derive it with \code{resolve_rooting_outgroup()} rather than naming a terminal by hand.}

\item{seed}{Integer. Random seed for reproducible temporal bootstrap initialization. Defaults to \code{NULL} (random).}

\item{threads}{Integer. Number of CPU threads. Defaults to \code{4}.}

\item{workers}{Integer or NULL. Number of parallel tree search workers. If \code{NULL}, auto-configured. Defaults to \code{NULL}.}

\item{blopt}{Character. Branch length optimization method (\code{"nr_safe"} or \code{"nr_fast"}). Defaults to \code{"nr_safe"} for robust numerical convergence across partitioned alignments.}

\item{output_dir}{Character. Directory path to save output temporal bootstrap trees. Defaults to \code{dirname(aln_file)}.}

\item{prefix}{Character. Output file prefix. Defaults to \code{"cactus_temporal"}.}
}
\value{
Character path to the resulting temporal bootstrap trees file (\code{.raxml.bootstraps}).
}
\description{
Generates non-parametric bootstrap tree replicates where branch lengths are re-estimated while holding the focal maximum-likelihood topology constrained.
Temporal bootstraps propagate branch length uncertainty into downstream penalized likelihood divergence time estimation (\code{treePL}),
providing empirical confidence intervals for node ages without introducing topological variance.
}
