% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dating.R
\name{calibration_tips}
\alias{calibration_tips}
\title{Resolve the Terminals a Calibration Row Addresses}
\usage{
calibration_tips(constraints, column, value, tip_labels)
}
\arguments{
\item{constraints}{Data frame of the taxonomic table (\code{cactus_constraints.csv}), carrying a
\code{Specie_name} column and one column per rank used by the calibration table.}

\item{column}{Character. Name of the rank column the calibration row keys on.}

\item{value}{Character. One name, or several separated by \verb{;}.}

\item{tip_labels}{Character vector of terminals present in the tree.}
}
\value{
Character vector of terminals, intersected with \code{tip_labels}.
}
\description{
\code{treePL} locates a calibrated node by the MRCA of the terminals declared for it, so the taxon
set, not the row label, is what determines where a bound lands. \code{value} accepts a
semicolon-separated list of names, which the two stem calibrations require: a stem node is
shared by the families it subtends and cannot be addressed by a single family name.
}
\examples{
constraints <- data.frame(
  Specie_name = c("Opuntia_stricta", "Pereskia_aculeata", "Anacampseros_retusa"),
  Family = c("Cactaceae", "Cactaceae", "Anacampserotaceae")
)
calibration_tips(constraints, "Family", "Cactaceae;Anacampserotaceae",
                 constraints$Specie_name)
}
