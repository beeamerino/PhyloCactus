% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{check_bs_convergence}
\alias{check_bs_convergence}
\title{Check Bootstrap Convergence Criterion in RAxML-NG}
\usage{
check_bs_convergence(
  raxml_bin_path,
  bs_trees_file,
  bs_cutoff = 0.03,
  bs_metric = c("fbp", "tbe"),
  seed = NULL,
  threads = 4,
  output_dir = dirname(bs_trees_file),
  prefix = "cactus_bs_convergence"
)
}
\arguments{
\item{raxml_bin_path}{Character. System command or full path to executable \code{RAxML-NG} binary.}

\item{bs_trees_file}{Character. Path to concatenated bootstrap tree file (e.g., \code{cactus_ALL_bootstraps.tree}).}

\item{bs_cutoff}{Numeric. Permutation cutoff threshold for convergence. Defaults to \code{0.03}.}

\item{bs_metric}{Character. Branch support metric for the bootstopping test: \code{"fbp"} (Felsenstein's Bootstrap Percentage, the default) or \code{"tbe"} (Transfer Bootstrap Expectation). Must match the metric being reported.}

\item{seed}{Integer. Random seed for reproducible convergence testing. Defaults to \code{NULL} (random).}

\item{threads}{Integer. Number of CPU threads. Defaults to \code{4}.}

\item{output_dir}{Character. Directory path to save convergence report logs. Defaults to \code{dirname(bs_trees_file)}.}

\item{prefix}{Character. Output file prefix. Defaults to \code{"cactus_bs_convergence"}.}
}
\value{
A structured S3 object of class \code{cactus_bs_convergence} containing:
\itemize{
\item \code{converged}: Logical indicating whether the autoMRE convergence criterion was satisfied.
\item \code{trees_at_convergence}: Integer number of trees required to reach convergence (or \code{NA} if not converged).
\item \code{trees_analyzed}: Total number of bootstrap trees evaluated.
\item \code{cutoff}: Numeric convergence cutoff threshold.
\item \code{metric}: Support metric used (\code{"fbp"} or \code{"tbe"}).
\item \code{log_file}: Character path to the resulting RAxML-NG convergence log file (\code{.raxml.log}).
}
}
\description{
Evaluates whether the generated pool of non-parametric bootstrap trees has achieved statistical
convergence (\code{RAxML-NG}), using the autoMRE bootstopping criterion at a permutation cutoff
(typically 0.03).
}
\details{
The criterion is metric-specific and the two metrics do not converge at the same replicate count.
TBE is the more stable summary and reaches the cutoff earlier, so a pool declared converged under
TBE is not thereby converged under FBP. The default is FBP, matching \code{map_branch_supports()}, so
that the convergence statement and the reported support values refer to the same quantity. Run the
test under whichever metric is being reported, and under both when both are tabulated.
}
