% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barcoding_classifier.R
\name{classify_barcoding_folds}
\alias{classify_barcoding_folds}
\title{Classify the Validation Folds of the Molecular Diagnostic Branch}
\usage{
classify_barcoding_folds(
  library_dir = file.path("11_barcoding", "4_library"),
  folds_dir = file.path("11_barcoding", "5_folds"),
  output_dir = file.path("11_barcoding", "7_classifier"),
  method = c("nn", "idtaxa"),
  model = "raw",
  min_comparable = 100L,
  schemes = c("species", "genus"),
  loci = NULL,
  max_folds = NULL,
  seed = 1L,
  threshold = 60,
  chunk = NULL,
  n_chunks = NULL,
  alignment = c("library", "add"),
  mafft_exec = "mafft",
  mafft_opts = "--auto",
  notify = FALSE,
  notify_to = NULL,
  notify_credentials = NULL
)
}
\arguments{
\item{library_dir}{Character. Output directory of \code{\link[=finalize_barcoding_library]{finalize_barcoding_library()}}.}

\item{folds_dir}{Character. Output directory of \code{\link[=build_barcoding_folds]{build_barcoding_folds()}}.}

\item{output_dir}{Character. Destination; refused inside a directory of the phylogeny.}

\item{method}{Character. \code{"nn"}, the branch classifier, or \code{"idtaxa"}, the contrast.}

\item{model}{Character. Distance model of \code{\link[ape:dist.dna]{ape::dist.dna()}}, for \code{"nn"}.}

\item{min_comparable}{Integer. Minimum number of comparable positions for a pair to have a value
(E13 of the validation plan). Pairs below it are left without value, never imputed.}

\item{schemes}{Character vector. \code{"species"} (scheme E) and \code{"genus"} (scheme G).}

\item{loci}{Character vector or \code{NULL} for every locus of the library.}

\item{max_folds}{Integer or \code{NULL}. Folds per locus and scheme; a declared random subset, for the
contrast with IdTaxa, which retrains once per fold. \code{NULL} means all of them.}

\item{seed}{Integer. Seed of that subset, so it is reproducible, and, for \code{"idtaxa"}, the seed
from which the seed of every query is derived with its locus, scheme, fold and \code{sid}, so that
each answer is reproducible and independent of the rest of the run.}

\item{threshold}{Numeric. Confidence threshold applied to the output of \code{\link[DECIPHER:IdTaxa]{DECIPHER::IdTaxa()}}, for
\code{"idtaxa"}. IdTaxa itself always runs at threshold 0 and the confidence of each rank is written
(\code{genus_confidence}, \code{species_confidence}), so any other threshold can be applied afterwards
with the same result as a run at that threshold.}

\item{chunk, n_chunks}{Integers, or \code{NULL} for a run without chunks. With both, only chunk
\code{chunk} of \code{n_chunks} is classified: inside each locus and scheme the folds, in the order of
step 5, are dealt round-robin to the chunks. The chunk writes its tables in \verb{chunks/} of
\code{output_dir}, with the suffix \verb{_chunk<k>of<n>}, together with its session; the tables of a whole
run are then written by \code{\link[=merge_barcoding_chunks]{merge_barcoding_chunks()}}. For a job array on a cluster, see
\code{\link[=generate_barcoding_job_scripts]{generate_barcoding_job_scripts()}}.}

\item{alignment}{Character. How the distance of a query is measured, for \code{"nn"}. \code{"library"},
the default, reads it from the joint alignment of the library, in which the query took part.
\code{"add"} removes the query's gaps, orients it against the training set of its fold and adds it
to that training alignment with \code{MAFFT --add --keeplength}, one call per query: the path of CN2
and of a query brought by a user. The probe of 2026-09-25 found the two paths agreeing on 699 of
720 queries and 4 answers changed. \code{"add"} writes its own tables, with the suffix \verb{_add}, and
never touches those of \code{"library"}.}

\item{mafft_exec, mafft_opts}{Command of the \code{MAFFT} binary and its options, for \code{alignment = "add"}.}

\item{notify}{Logical. Send an email when the run ends, whether it finished or failed. Intended
for \code{method = "idtaxa"}, which retrains once per fold and is measured in hours. A notification
that cannot be sent is reported and ignored: it never fails the run. See
\code{\link[=send_run_notification]{send_run_notification()}} for what has to be configured. Defaults to \code{FALSE}.}

\item{notify_to, notify_credentials}{Passed to \code{\link[=send_run_notification]{send_run_notification()}} as \code{to} and
\code{credentials}. Both default to \code{NULL}, which reads the \code{MY_EMAIL} and \code{PHYLOCACTUS_SMTP_CREDS}
environment variables.}
}
\value{
Invisibly, a list of prediction tables by scheme. Writes
\verb{TABLE_barcoding_predictions_<scheme>_<method>.csv} and \verb{TABLE_barcoding_timing_<method>.csv},
one row per locus and scheme with the folds, the queries and the seconds they took; with
\code{alignment = "add"} both names carry the suffix \verb{_add} and the prediction tables add the columns
\code{alignment} and \code{orientation}.
}
\description{
Step 7 of the branch. Reads the library of step 4 and the folds of step 5, classifies every query
of every fold and writes the raw predictions, one row per query. It aggregates nothing: no
accuracy, no rate, no mean. The negative controls (Phase 5B) and the real metrics (Phase 6) read
these tables afterwards.
}
\details{
The output has three states (validation plan, sec. 1): species, genus with the specific ambiguity
declared, and not assignable. The state comes from the structure of the tie at the minimum
distance, not from a threshold, so the threshold can be swept later over \code{nn_distance},
\code{margin} and \code{confidence} without classifying again.
}
\examples{
\dontrun{
classify_barcoding_folds(
  library_dir = "11_barcoding/4_library",
  folds_dir = "11_barcoding/5_folds",
  output_dir = "11_barcoding/7_classifier"
)
}
}
