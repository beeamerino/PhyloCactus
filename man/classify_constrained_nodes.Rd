% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support-audit.R
\name{classify_constrained_nodes}
\alias{classify_constrained_nodes}
\title{Classify Every Internal Node as Constrained or Estimated}
\usage{
classify_constrained_nodes(tree, constraint_tree)
}
\arguments{
\item{tree}{An \code{ape} \code{phylo} object, or a path to a Newick file. Pass the annotated support tree
(\code{.raxml.support}) to carry support values through into the result.}

\item{constraint_tree}{An \code{ape} \code{phylo} object, or a path to a Newick file, holding the topology
passed to \code{RAxML-NG --tree-constraint}. Terminals absent from \code{tree} (collapsed as identical
during parsing) are dropped before comparison.}
}
\value{
A \code{data.frame} with one row per internal node: \code{node} (index in the \code{ape} numbering),
\code{n_tips} (size of the smaller side of the bipartition), \code{constrained} (logical) and \code{support}
(numeric, \code{NA} when the tree carries no node labels).
}
\description{
Compares the bipartitions of a reference topology against those of the topological constraint it
was inferred under, and reports for each internal node whether the split was imposed or estimated.
Support values attached to imposed splits are returned unchanged but must not be reported as
support: when the bootstrap replicates were themselves run with \code{--tree-constraint}, every
replicate reproduces those splits and the value is 1 by construction.
}
\examples{
\dontrun{
nodes <- classify_constrained_nodes(
  tree            = "7_Phylogenetics/cactus_support_fbp.raxml.support",
  constraint_tree = "7_Phylogenetics/cactus_constraints.tree"
)
table(nodes$constrained)
summary(nodes$support[!nodes$constrained])
}
}
