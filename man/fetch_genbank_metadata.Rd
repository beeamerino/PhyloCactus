% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assembly.R
\name{fetch_genbank_metadata}
\alias{fetch_genbank_metadata}
\title{Fetch GenBank Sequence Metadata via NCBI Entrez Utilities}
\usage{
fetch_genbank_metadata(
  sids,
  cache_file,
  batch_size = 200,
  sleep_time = 0.5,
  max_retries = 5,
  force_download = FALSE
)
}
\arguments{
\item{sids}{Character vector of GenBank Sequence Identifiers (SIDs) to query.}

\item{cache_file}{Character. File path to store and load cached metadata tables.}

\item{batch_size}{Integer. Number of sequence IDs requested per HTTP batch query. Defaults to \code{200}.}

\item{sleep_time}{Numeric. Pause duration in seconds between consecutive batch requests to respect NCBI rate limits. Defaults to \code{0.5}.}

\item{max_retries}{Integer. Maximum retry attempts permitted per batch before failing. Defaults to \code{5}.}

\item{force_download}{Logical. Not used in this version: records already in \code{cache_file} are always reused. Defaults to \code{FALSE}.}
}
\value{
A data frame with columns \code{sid}, \code{Species_gb} and \code{Description_gb}.
}
\description{
Retrieves the organism name and the definition line of each GenBank sequence identifier (SID)
with \code{ape::read.GenBank()}, in batches with retries, and caches the result.
}
\examples{
\dontrun{
fetch_genbank_metadata(
  sids = c("AY123456", "AY123457"),
  cache_file = "cache_metadata.csv"
)
}
}
