% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barcoding_library.R
\name{finalize_barcoding_library}
\alias{finalize_barcoding_library}
\title{Build the Final Reference Library of the Molecular Diagnostic Branch}
\usage{
finalize_barcoding_library(
  assembly_dir = file.path("11_barcoding", "1_assembly"),
  curated_dir = file.path("11_barcoding", "2_curated"),
  screening_dir = file.path("11_barcoding", "3_screening"),
  output_dir = file.path("11_barcoding", "4_library"),
  metadata_file = file.path("11_barcoding", "cache",
    "CACHE_GENBANK_METADATA_BARCODING.csv"),
  min_species_with_replica = 20L,
  paralog_loci = "pepC_like"
)
}
\arguments{
\item{assembly_dir}{Character. Output directory of \code{\link[=assemble_barcoding_dataset]{assemble_barcoding_dataset()}}; the accession
registry is read from it.}

\item{curated_dir}{Character. Output directory of \code{\link[=curate_barcoding_markers]{curate_barcoding_markers()}}; the curated
alignments and the \verb{LOG_STRAND_<locus>.csv} files are read from it.}

\item{screening_dir}{Character. Output directory of \code{\link[=screen_barcoding_markers]{screen_barcoding_markers()}}; the loci that
entered the screening are read from \code{TABLE_barcoding_marker_screening.csv}.}

\item{output_dir}{Character. Destination; refused inside a directory of the phylogeny.}

\item{metadata_file}{Character. GenBank metadata cache written by \code{\link[=assemble_barcoding_dataset]{assemble_barcoding_dataset()}},
read for the description of each excluded sequence.}

\item{min_species_with_replica}{Integer. A locus enters the library when at least this number of
species keep two or more distinct sequences. Defaults to \code{20L} (decision of 2026-09-21).}

\item{paralog_loci}{Character vector. Loci under paralogy surveillance, flagged in the column
\code{posible_paralogo} of every table; the flag never excludes a locus. Defaults to \code{"pepC_like"}.}
}
\value{
Invisibly, a list with \code{summary}, \code{excluded} and \code{collapsed}. Writes \verb{LIB_<locus>.fasta}
for every locus that enters, \code{TABLE_barcoding_library_summary.csv},
\code{TABLE_barcoding_excluded_nonhomologous.csv}, \code{TABLE_barcoding_collapsed_identical.csv} and
\code{TABLE_barcoding_funnel.csv} (per locus: accessions assembled, curated, excluded, collapsed and
kept, species with replica, and whether the locus entered the screening and the library).
}
\description{
Takes the curated alignments written by \code{\link[=curate_barcoding_markers]{curate_barcoding_markers()}} and writes the reference
library used by the validation. For each locus it (1) removes the sequences that the alignment
pipeline logged as matching the locus in neither direction (\code{no_match_either_direction} in
\verb{LOG_STRAND_<locus>.csv}), which are paralogs or other non-homologous copies, not intraspecific
replication; (2) collapses accessions with the same aligned sequence within each species to the
alphabetically smallest sid; (3) evaluates the replication threshold again, counting a species as
replicated when it keeps two or more distinct sequences. Identical sequences of different species
are kept. The columns of the curated alignment are not modified, so removing sequences can leave
columns made only of gaps.
}
\details{
Every input is read from the directory of the step that wrote it, so the library runs on its own
whenever those directories exist. The loci are those marked \code{entra} in the screening table.
}
\examples{
\dontrun{
lib <- finalize_barcoding_library(
  assembly_dir = "11_barcoding/1_assembly",
  curated_dir = "11_barcoding/2_curated",
  screening_dir = "11_barcoding/3_screening"
)
}
}
