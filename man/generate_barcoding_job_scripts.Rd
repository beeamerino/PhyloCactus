% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barcoding_chunks.R
\name{generate_barcoding_job_scripts}
\alias{generate_barcoding_job_scripts}
\title{Generate the SLURM Job Scripts of a Chunked Run of Step 7}
\usage{
generate_barcoding_job_scripts(
  library_dir,
  folds_dir,
  classifier_dir,
  method = c("idtaxa", "nn"),
  n_chunks,
  job_dir = file.path(classifier_dir, "job"),
  seed = 1L,
  threshold = 60,
  schemes = c("species", "genus"),
  loci = NULL,
  max_folds = NULL,
  cluster_job_name = "cactus_idtaxa",
  cluster_partition = "main",
  cluster_mem = "4G",
  cluster_time = "04:00:00",
  cluster_queue = NULL,
  cluster_mail_user = Sys.getenv("MY_EMAIL", ""),
  load_module = NULL,
  r_lib = NULL,
  rscript_exec = "Rscript"
)
}
\arguments{
\item{library_dir, folds_dir}{Character. As in \code{\link[=classify_barcoding_folds]{classify_barcoding_folds()}}, on the cluster.}

\item{classifier_dir}{Character. \code{output_dir} of the run, on the cluster.}

\item{method, seed, threshold, schemes, loci, max_folds}{As in \code{\link[=classify_barcoding_folds]{classify_barcoding_folds()}}.}

\item{n_chunks}{Integer. Number of chunks, one array task each.}

\item{job_dir}{Character. Where the scripts are written. Defaults to \verb{job/} inside
\code{classifier_dir}.}

\item{cluster_job_name}{Character. SLURM job name; the merge job adds \verb{_merge}. Defaults to
\code{"cactus_idtaxa"}.}

\item{cluster_partition}{Character. SLURM partition. Defaults to \code{"main"}.}

\item{cluster_mem}{Character. Memory per task (\code{--mem}). Defaults to \code{"4G"}.}

\item{cluster_time}{Character. Time limit per task (\code{-t}). Defaults to \code{"04:00:00"}.}

\item{cluster_queue}{Character. Optional SLURM QoS (\code{-q}). Defaults to \code{NULL}.}

\item{cluster_mail_user}{Character. Address for SLURM mail. Defaults to
\code{Sys.getenv("MY_EMAIL", "")}; an empty value writes no mail line. The array mails on failure
only; the merge mails at its end.}

\item{load_module}{Character vector of environment modules to load, or \code{NULL}.}

\item{r_lib}{Character. R library to put first (\code{R_LIBS_USER}), or \code{NULL}.}

\item{rscript_exec}{Character. The \code{Rscript} command. Defaults to \code{"Rscript"}.}
}
\value{
Invisibly, the paths of the five files.
}
\description{
Writes the scripts that run \code{\link[=classify_barcoding_folds]{classify_barcoding_folds()}} as a SLURM job array, one chunk per
task on one core, and \code{\link[=merge_barcoding_chunks]{merge_barcoding_chunks()}} once every task has finished without error. It
follows the conventions of \code{\link[=generate_ml_search_script]{generate_ml_search_script()}} and \code{\link[=generate_bootstrap_script]{generate_bootstrap_script()}}:
\verb{cluster_*} arguments, \code{load_module}, one log per array task, and the versions the job ran with
printed at its start.
}
\details{
Five files are written in \code{job_dir}: \code{run_chunk.R} and \code{run_merge.R}, which hold the arguments of
the run; \code{job_array.sh}, the array (\code{--array=1-n_chunks}, one core per task); \code{job_merge.sh}, the
merge; and \code{submit.sh}, which submits the array and then the merge with
\verb{--dependency=afterok:<array>}. Submit with \verb{bash submit.sh} from any directory.

The paths are written as given and must be those of the cluster: pass absolute paths, or paths
relative to the directory the job is submitted from. Nothing about a particular cluster is
written unless it is passed: with \code{load_module = NULL} no module is loaded, with
\code{cluster_queue = NULL} no QoS is requested, and with \code{r_lib = NULL} the default R library is used.
}
\examples{
\dontrun{
generate_barcoding_job_scripts(
  library_dir = "/home/user/PhyloCactus_Tutorial/11_barcoding/4_library",
  folds_dir = "/home/user/PhyloCactus_Tutorial/11_barcoding/5_folds",
  classifier_dir = "/home/user/PhyloCactus_Tutorial/11_barcoding/7_classifier",
  method = "idtaxa", n_chunks = 60,
  load_module = "R/4.4.0", r_lib = "~/R/phylocactus/4.4"
)
}
}
\seealso{
\code{\link[=classify_barcoding_folds]{classify_barcoding_folds()}}, \code{\link[=merge_barcoding_chunks]{merge_barcoding_chunks()}}.
}
