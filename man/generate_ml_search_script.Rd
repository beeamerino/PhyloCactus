% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{generate_ml_search_script}
\alias{generate_ml_search_script}
\title{Generate a SLURM Batch Script for the Constrained Maximum-Likelihood Search}
\usage{
generate_ml_search_script(
  alignment_file,
  partition_file,
  constraint_file,
  outgroup = NULL,
  n_init_trees = "rand{25},pars{25}",
  seed = NULL,
  threads = 75,
  workers = NULL,
  min_threads_per_worker = 3L,
  preparse = TRUE,
  output_dir = getwd(),
  script_name = "run_ml_search.sh",
  prefix = "cactus_search",
  cluster_job_name = "cactus_ml",
  cluster_partition = "main",
  cluster_nodes = 1L,
  cluster_mem = "16G",
  cluster_time = "02:00:00",
  cluster_queue = NULL,
  cluster_mail_user = Sys.getenv("MY_EMAIL", ""),
  load_module = c("gcc/14.2.0-nlhpc", "openmpi/5.0.3-o", "raxml-ng/1.1.0-mpi-zen4-n"),
  raxml_exec = "raxml-ng-mpi"
)
}
\arguments{
\item{alignment_file}{Character. Path to input PHYLIP supermatrix alignment file.}

\item{partition_file}{Character. Path to partition file specifying substitution models per partition.}

\item{constraint_file}{Character. Path to Newick topological constraint scaffold file.}

\item{outgroup}{Character vector of terminals passed to \code{RAxML-NG --outgroup}, or \code{NULL} (default); multiple terminals are joined with commas. \code{RAxML-NG} writes an unrooted topology with these terminals placed first, so this argument orders the output rather than rooting the tree: the root is imposed downstream by \code{automate_treePL()} via \code{ape::root(..., resolve.root = TRUE)}. Declaring the same set at every stage keeps the output ordering consistent across the maximum-likelihood search, the bootstrap replicates and the temporal bootstraps. Derive it with \code{resolve_rooting_outgroup()} rather than naming a terminal by hand.}

\item{n_init_trees}{Character. Starting tree specification passed to \code{--tree}. Defaults to \verb{"rand{25},pars{25}"}.}

\item{seed}{Integer. Random seed for reproducible tree search initialization. Defaults to \code{NULL} (random).}

\item{threads}{Integer. CPU cores requested per SLURM task (\code{--cpus-per-task}). Defaults to \code{120}.}

\item{workers}{Integer. Worker process count. If \code{NULL} (default), derived from \code{threads},
\code{n_init_trees} and \code{min_threads_per_worker} so that workers divide the starting tree count evenly.}

\item{min_threads_per_worker}{Integer. Lower bound on threads per worker when \code{workers} is derived. Defaults to \code{4L}.}

\item{preparse}{Logical. Pre-parse the alignment into compressed binary \code{.rba} format before the search? Defaults to \code{TRUE}.}

\item{output_dir}{Character. Destination directory for the script and its outputs. Defaults to \code{getwd()}.}

\item{script_name}{Character. Name of the generated Bash script. Defaults to \code{"run_ml_search.sh"}.}

\item{prefix}{Character. RAxML-NG output prefix inside the job. Defaults to \code{"cactus_search"}.}

\item{cluster_job_name}{Character. SLURM job name. Defaults to \code{"cactus_ml"}.}

\item{cluster_partition}{Character. SLURM partition. Defaults to \code{"main"}.}

\item{cluster_nodes}{Integer. Number of compute nodes requested (\verb{--nodes}). Defaults to \code{1L}.}

\item{cluster_mem}{Character. Memory allocation (\code{--mem}). Defaults to \code{"64G"}.}

\item{cluster_time}{Character. Time limit (\code{--time}). Defaults to \code{"12:00:00"}.}

\item{cluster_queue}{Character. Optional SLURM queue / QoS. Defaults to \code{NULL}.}

\item{cluster_mail_user}{Character. Notification recipient email address for SLURM (\verb{--mail-user}). Defaults to \code{Sys.getenv("MY_EMAIL", "")}.}

\item{load_module}{Character vector. Environment modules to load. Defaults to the NLHPC Leftraru
\code{RAxML-NG} toolchain.}

\item{raxml_exec}{Character. \code{RAxML-NG} executable invoked inside the job. Defaults to \code{"raxml-ng-mpi"}.}
}
\value{
Character path to the generated SLURM batch script, carrying the resolved parallel plan
as the \code{ml_plan} attribute.
}
\description{
Writes a single SLURM batch script running the same constrained \code{RAxML-NG} search as
\code{\link[=calculate_ml_tree]{calculate_ml_tree()}}, sized for a compute node instead of a workstation. The search is one job
rather than a job array: independent starting-tree searches are distributed across \code{--workers}
inside the job, and RAxML-NG writes one \code{.raxml.bestTree} directly, so no collection step is
needed to compare log-likelihoods across tasks.
}
\details{
Wall-clock is governed by the number of sequential rounds each worker must run,
\code{ceiling(n_trees / workers)}, not by the thread count alone. Total core-work is fixed, so adding
threads to a single worker shortens each tree while adding workers shortens the number of rounds,
and the second lever is the one that matters once a search is already using an efficient thread
count per worker. Running 50 starting trees on one worker executes 50 rounds; the same 50 trees
across 25 workers execute 2.

Reference timing for this workload, measured on an Apple M2 Pro (8 threads, \verb{--workers 1},
\code{RAxML-NG} 1.2.2, SSE3 kernels) over a supermatrix of 986 terminals, 12700 sites, 5732 patterns
and 11 partitions: 50 starting trees in 47115 s, approximately 938 s per tree. That figure is a
per-worker baseline for one tree at 8 threads and does not transfer directly to a different
architecture or SIMD kernel; treat it as an order of magnitude and confirm with a short trial run
(\code{n_init_trees = "rand{2}"}) before committing a full allocation.

\code{workers} is derived automatically and constrained to a divisor of the starting tree
count, so that no worker sits idle in the final round.
}
\examples{
\dontrun{
generate_ml_search_script(
  alignment_file = "cactus.raxml.reduced.phy",
  partition_file = "cactus_modeltest.part.aicc",
  constraint_file = "cactus_constraints.tree",
  outgroup = resolve_rooting_outgroup(ape::read.tree("bestTree.tree")$tip.label),
  output_dir = "7_Phylogenetics"
)
}
}
\references{
Kozlov, A. M., Darriba, D., Flouri, T., Morel, B., & Stamatakis, A. (2019). RAxML-NG: a fast, scalable and
user-friendly tool for maximum likelihood phylogenetic inference. \emph{Bioinformatics}, 35(21), 4453-4455.
\doi{10.1093/bioinformatics/btz305}
}
\seealso{
\code{\link[=calculate_ml_tree]{calculate_ml_tree()}} for the equivalent local run.
}
