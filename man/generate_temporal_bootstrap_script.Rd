% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{generate_temporal_bootstrap_script}
\alias{generate_temporal_bootstrap_script}
\title{Generate High-Performance Computing (HPC/SLURM) Batch Script for Temporal Bootstrapping}
\usage{
generate_temporal_bootstrap_script(
  alignment_file,
  partition_file,
  best_tree_file,
  outgroup = NULL,
  bs_trees = 500,
  base_seed = NULL,
  threads = 40,
  workers = NULL,
  threads_per_worker = 4L,
  preparse = TRUE,
  output_dir = getwd(),
  script_name = "run_temporal_bs.sh",
  cluster_job_name = "cactus_temp_bs",
  cluster_partition = "main",
  cluster_nodes = 1L,
  cluster_mem = "16G",
  cluster_time = "04:00:00",
  cluster_queue = NULL,
  cluster_mail_user = Sys.getenv("MY_EMAIL", ""),
  load_module = c("gcc/14.2.0-nlhpc", "openmpi/5.0.3-o", "raxml-ng/1.1.0-mpi-zen4-n"),
  raxml_exec = "raxml-ng-mpi"
)
}
\arguments{
\item{alignment_file}{Character. Path to input PHYLIP alignment file.}

\item{partition_file}{Character. Path to partition file specifying substitution models.}

\item{best_tree_file}{Character. Path to best scoring maximum-likelihood tree topology file (used as constraint).}

\item{outgroup}{Character vector of terminals passed to \code{RAxML-NG --outgroup}, or \code{NULL} (default); multiple terminals are joined with commas. \code{RAxML-NG} writes an unrooted topology with these terminals placed first, so this argument orders the output rather than rooting the tree: the root is imposed downstream by \code{automate_treePL()} via \code{ape::root(..., resolve.root = TRUE)}. Declaring the same set at every stage keeps the output ordering consistent across the maximum-likelihood search, the bootstrap replicates and the temporal bootstraps. Derive it with \code{resolve_rooting_outgroup()} rather than naming a terminal by hand.}

\item{bs_trees}{Integer. Total number of temporal bootstrap trees to generate. Defaults to \code{500}.}

\item{base_seed}{Integer. Random seed for reproducible initialization. Defaults to \code{NULL} (random).}

\item{threads}{Integer. Number of CPU cores requested per SLURM task (\code{--cpus-per-task}). Defaults to \code{40}.}

\item{workers}{Integer or NULL. Number of RAxML-NG worker processes. If \code{NULL} (default), calculated dynamically as \code{max(1L, as.integer(threads / threads_per_worker))}.}

\item{threads_per_worker}{Integer. Thread-to-worker ratio used to derive \code{workers} when \code{workers = NULL}. Defaults to \code{4L}.}

\item{preparse}{Logical. Generate compressed binary \code{.rba} format with \code{raxml-ng --parse} prior to execution to minimize disk I/O contention? Defaults to \code{TRUE}.}

\item{output_dir}{Character. Destination directory for script and outputs. Defaults to \code{getwd()}.}

\item{script_name}{Character. Name of output Bash script file. Defaults to \code{"run_temporal_bs.sh"}.}

\item{cluster_job_name}{Character. SLURM job name identifier. Defaults to \code{"cactus_temp_bs"}.}

\item{cluster_partition}{Character. SLURM partition name. Defaults to \code{"main"}.}

\item{cluster_nodes}{Integer. Number of compute nodes requested (\code{--nodes}). Defaults to \code{1L}.}

\item{cluster_mem}{Character. Memory allocation string for SLURM (\code{--mem}). Defaults to \code{"16G"}.}

\item{cluster_time}{Character. Time limit allocation string for SLURM (\code{--time}). Defaults to \code{"04:00:00"}.}

\item{cluster_queue}{Character. Optional SLURM queue / QoS name. Defaults to \code{NULL}.}

\item{cluster_mail_user}{Character. Notification recipient email address for SLURM (\code{--mail-user}). Defaults to \code{Sys.getenv("MY_EMAIL", "")}.}

\item{load_module}{Character vector or string. Environment module(s) to load prior to execution. Defaults to \code{c("gcc/14.2.0-nlhpc", "openmpi/5.0.3-o", "raxml-ng/1.1.0-mpi-zen4-n")}.}

\item{raxml_exec}{Character. Executable \code{RAxML-NG} binary command. Defaults to \code{"raxml-ng-mpi"}.}
}
\value{
Character path to the generated SLURM batch script file.
}
\description{
Writes a self-contained SLURM batch script for running high-throughput temporal bootstrap replicate inference
using MPI-enabled \code{RAxML-NG} (\code{raxml-ng-mpi}) on a compute cluster. Temporal bootstraps constrain the maximum-likelihood
tree topology and re-estimate branch lengths across bootstrap matrices, generating empirical distributions of branch
lengths for downstream divergence time estimation (\code{treePL}) without introducing topological discordance.
}
