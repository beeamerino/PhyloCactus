% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{map_branch_supports}
\alias{map_branch_supports}
\title{Map Bootstrap Support Values onto Reference Phylogeny}
\usage{
map_branch_supports(
  raxml_bin,
  best_tree,
  bootstraps_file,
  metric = c("fbp", "tbe"),
  threads = 4,
  output_dir = dirname(best_tree),
  prefix = "cactus_support"
)
}
\arguments{
\item{raxml_bin}{Character. System command or full path to executable \code{RAxML-NG} binary.}

\item{best_tree}{Character. Path to reference maximum-likelihood tree file.}

\item{bootstraps_file}{Character. Path to concatenated non-parametric bootstrap trees file.}

\item{metric}{Character. Bootstrap support metric: \code{"fbp"} (Felsenstein's Bootstrap Percentage, the default and the comparable metric) or \code{"tbe"} (Transfer Bootstrap Expectation, secondary).}

\item{threads}{Integer. Number of CPU threads. Defaults to \code{4}.}

\item{output_dir}{Character. Output directory for annotated support tree. Defaults to \code{dirname(best_tree)}.}

\item{prefix}{Character. Output file prefix. Defaults to \code{"cactus_support"}.}
}
\value{
Character path to the annotated support tree file (\code{.raxml.support}).
}
\description{
Maps clade support derived from non-parametric bootstrap replicates onto the best maximum-likelihood
tree topology. The replicates themselves carry no metric: \code{RAxML-NG --bootstrap} writes plain
topologies with branch lengths, and the metric is chosen here, at the summarising step. The same
replicate file can therefore be summarised under both metrics without recomputation.
}
\details{
Defaults to Felsenstein's Bootstrap Percentage (FBP; Felsenstein, 1985), which is the metric the
Cactaceae and Caryophyllales dating literature reports and the only one against which this tree can
be compared. Transfer Bootstrap Expectation (TBE; Lemoine \emph{et al.}, 2018) is available through
\code{metric = "tbe"} and belongs in a clearly labelled secondary column.

The two are not on a common scale and TBE must never be reported as though it were a bootstrap
percentage. TBE is bounded below by FBP and its inflation grows with clade size. Measured on the
986 unconstrained nodes of the 1023 terminal supermatrix tree (2026-09-06): median TBE 0.783
against median FBP 0.468, with the gap reaching 0.704 for clades of 51 to 200 terminals. Reporting
TBE would place 61 percent of nodes above 0.70; FBP places 29 percent.

Support values are meaningless for any bipartition imposed through \code{--tree-constraint}, because
every replicate reproduces it by construction. Those nodes must be reported as constrained rather
than supported. Use \code{classify_constrained_nodes()} to separate them before tabulating support.
}
\examples{
\dontrun{
# Primary metric, comparable with the published literature.
map_branch_supports(
  raxml_bin = "raxml-ng",
  best_tree = "cactus_search.raxml.bestTree",
  bootstraps_file = "cactus_ALL_bootstraps.tree",
  metric = "fbp",
  prefix = "cactus_support_fbp"
)

# Secondary metric, same replicates, no recomputation.
map_branch_supports(
  raxml_bin = "raxml-ng",
  best_tree = "cactus_search.raxml.bestTree",
  bootstraps_file = "cactus_ALL_bootstraps.tree",
  metric = "tbe",
  prefix = "cactus_support_tbe"
)
}
}
\references{
Felsenstein, J. (1985). Confidence limits on phylogenies: an approach using the bootstrap.
\emph{Evolution}, 39(4), 783-791. \doi{10.1111/j.1558-5646.1985.tb00420.x}

Lemoine, F., Domelevo Entfellner, J. B., Wilkinson, E., Correia, D., Davila Felipe, M.,
De Oliveira, T., & Gascuel, O. (2018). Renewing Felsenstein's phylogenetic bootstrap in the era of
big data. \emph{Nature}, 556(7702), 452-456. \doi{10.1038/s41586-018-0043-0}
}
