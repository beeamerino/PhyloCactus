% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barcoding_chunks.R
\name{merge_barcoding_chunks}
\alias{merge_barcoding_chunks}
\title{Merge the Chunks of a Chunked Run of Step 7}
\usage{
merge_barcoding_chunks(
  library_dir = file.path("11_barcoding", "4_library"),
  folds_dir = file.path("11_barcoding", "5_folds"),
  output_dir = file.path("11_barcoding", "7_classifier"),
  method = c("nn", "idtaxa"),
  n_chunks,
  alignment = c("library", "add"),
  schemes = c("species", "genus"),
  loci = NULL,
  max_folds = NULL,
  seed = 1L
)
}
\arguments{
\item{library_dir, folds_dir, output_dir, method, alignment, schemes, loci, max_folds, seed}{As in
\code{\link[=classify_barcoding_folds]{classify_barcoding_folds()}}, with the values the chunks were run with.}

\item{n_chunks}{Integer. Number of chunks of the run.}
}
\value{
Invisibly, a list of prediction tables by scheme. Writes
\verb{TABLE_barcoding_predictions_<scheme>_<method>.csv} and \verb{TABLE_barcoding_timing_<method>.csv}
in \code{output_dir}.
}
\description{
Reads the tables that \code{classify_barcoding_folds(chunk = k, n_chunks = n)} wrote in \verb{chunks/} of
\code{output_dir}, checks them against the folds of step 5 and writes the tables of the whole run,
which are the tables a run without chunks would have written, row for row and in the same order.
}
\details{
The merge refuses, naming what it found, when a chunk is missing, when a fold appears that is not
among the folds planned for the run (the folds of step 5, with the declared sample of
\code{max_folds}), when a query appears more than once, and when a planned query is missing. The
arguments that choose the folds (\code{schemes}, \code{loci}, \code{max_folds}, \code{seed}) must be those of the
chunks. The timing table sums the seconds of the chunks by locus and scheme; the chunk tables and
their sessions are left in \verb{chunks/}.
}
\examples{
\dontrun{
merge_barcoding_chunks(
  library_dir = "11_barcoding/4_library",
  folds_dir = "11_barcoding/5_folds",
  output_dir = "11_barcoding/7_classifier",
  method = "idtaxa", n_chunks = 60
)
}
}
\seealso{
\code{\link[=classify_barcoding_folds]{classify_barcoding_folds()}}, \code{\link[=generate_barcoding_job_scripts]{generate_barcoding_job_scripts()}}.
}
