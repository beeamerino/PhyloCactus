% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{preprocess_partitions}
\alias{preprocess_partitions}
\title{Preprocess Partitions and Validate Alignment Syntax}
\usage{
preprocess_partitions(
  phy_matrix,
  part_file,
  raxml_path,
  output_dir = dirname(phy_matrix),
  prefix = "cactus",
  force_check = FALSE,
  model_handling = c("force_dna", "preserve")
)
}
\arguments{
\item{phy_matrix}{Character. Path to input PHYLIP supermatrix file.}

\item{part_file}{Character. Path to input partition mapping text file in RAxML-style format
(\code{PARTITION_raxml_style.txt}, exported by \code{\link[=run_concatenation_pipeline]{run_concatenation_pipeline()}}).}

\item{raxml_path}{Character. System command or full path to executable \code{RAxML-NG} binary.}

\item{output_dir}{Character. Output directory for validated partition outputs. Defaults to \code{dirname(phy_matrix)}.}

\item{prefix}{Character. Basename prefix for every file written by this step. Defaults to \code{"cactus"}.}

\item{force_check}{Logical. Re-run validation even if the validated partition file already exists? Defaults to \code{FALSE}.}

\item{model_handling}{Character. Model field written to the validated partition map. \code{"force_dna"}
(default) writes the datatype token \code{DNA}, leaving the substitution model to be selected by
\code{\link[=run_modeltest_ng]{run_modeltest_ng()}}. \code{"preserve"} keeps whatever model string \code{RAxML-NG} emitted, which is only
appropriate when the partition map is passed straight to \code{RAxML-NG} without model selection.
\code{ModelTest-NG} cannot parse the \code{RAxML-NG} model syntax and aborts on it.}
}
\value{
Character path to the validated partition map file ready for model evaluation.
}
\description{
Validates PHYLIP alignment syntax and partition file coordinates using \code{RAxML-NG} (Kozlov \emph{et al.}, 2019).
Verifies site ranges, formatting compatibility, and data integrity prior to substitution model evaluation.
}
\references{
Kozlov, A. M., Darriba, D., Flouri, T., Morel, B., & Stamatakis, A. (2019). RAxML-NG: a fast, scalable and
user-friendly tool for maximum likelihood phylogenetic inference. \emph{Bioinformatics}, 35(21), 4453-4455.
\doi{10.1093/bioinformatics/btz305}
}
\seealso{
\code{\link[=run_concatenation_pipeline]{run_concatenation_pipeline()}} for the partition map this function consumes,
and \code{\link[=run_modeltest_ng]{run_modeltest_ng()}} for the model selection step that consumes its output.
}
