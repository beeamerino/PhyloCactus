% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concatenation.R
\name{report_marker_group_coverage}
\alias{report_marker_group_coverage}
\title{Report How Each Marker Covers the Ingroup and the Outgroup}
\usage{
report_marker_group_coverage(
  input_dir,
  outgroup_pattern,
  min_coverage = 0.2,
  out_csv = NULL,
  exclude_markers = NULL
)
}
\arguments{
\item{input_dir}{Character. Directory of aligned locus FASTA files, one per marker.}

\item{outgroup_pattern}{Character. Regular expression matched against terminal names, or a named
character vector of regular expressions (e.g. \code{c(Anacampserotaceae = "...", Portulacaceae = "...", Talinaceae = "...")})
to additionally compute per-group coverage columns.}

\item{min_coverage}{Numeric. Fraction of non-gap, non-missing characters at which a terminal
counts as covered by that marker. Defaults to \code{0.2}.}

\item{out_csv}{Character or \code{NULL}. Path to write the table to. Defaults to \code{NULL}.}

\item{exclude_markers}{Character vector or \code{NULL}. Markers to leave out of the report, named as
they appear in \code{input_dir} without the file extension and without any \code{Masked_} prefix. Passed
through by \code{\link[=run_concatenation_pipeline]{run_concatenation_pipeline()}} so that the coverage table describes the partitions
the supermatrix has, not every alignment the folder holds. Defaults to \code{NULL}.}
}
\value{
Invisibly, a data frame with one row per marker: alignment length, terminals and
covered terminals on each side, and the median coverage of each side.
}
\description{
Counts, per locus, how many ingroup and how many outgroup terminals carry real sequence, and
warns when either side is empty.
}
\details{
Concatenation assumes that the loci being joined describe the same terminals. A locus sampled
almost entirely on one side of the root breaks that assumption without breaking anything
visible: it adds columns the other side cannot share, and the branch lengths spanning the
bipartition are then estimated from the loci that remain. The branch subtending the outgroup
can then be badly underestimated, and the deepest calibrated nodes can return their own
bounds instead of an estimate.

The function reports and does not block. Whether a locus of that kind belongs in a given matrix
depends on which analysis the matrix is for: a nuclear locus with no outgroup coverage is
unusable for dating and valuable for species discrimination.
}
\examples{
\dontrun{
report_marker_group_coverage(
  input_dir = "5_MAFFT_Cleaned/aligned_markers",
  outgroup_pattern = c(
    Anacampserotaceae = "^(Anacampseros|Grahamia|Talinopsis)_",
    Portulacaceae     = "^Portulaca_",
    Talinaceae        = "^(Talinum|Talinella)_"
  )
)
}
}
