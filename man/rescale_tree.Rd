% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dating.R
\name{rescale_tree}
\alias{rescale_tree}
\title{Rescale Branch Lengths of a Phylogenetic Tree}
\usage{
rescale_tree(tree, factor = 100)
}
\arguments{
\item{tree}{An object of class \code{phylo} representing a phylogenetic tree.}

\item{factor}{Numeric multiplier applied to all edge lengths. Defaults to \code{100}.}
}
\value{
A rescaled \code{phylo} object with updated edge lengths.
}
\description{
Multiplies all edge lengths of a \code{phylo} tree object by a constant scaling factor.
}
\details{
The factor does not change which branches \code{treePL} clamps. \code{treePL} refuses to let a branch
carry less than one expected substitution and rewrites any shorter branch to \code{1/numsites},
so the clamp acts on the substitution count, not on the raw length. Measured on the Cactaceae
supermatrix, 477 of 2044 branches are clamped, identically at factor 100 and at factor 1, once
\code{numsites} is divided by the same factor as \code{automate_treePL()} does. What the factor does
change is the scale of the rate parameters and therefore the numerical conditioning of the
optimisation.

Earlier versions of this documentation described the rescaling as a guard against numerical
underflow. That is not what it does.
}
\examples{
\dontrun{
library(ape)
tree <- rtree(10)
rescaled_tree <- rescale_tree(tree, factor = 100)
}
}
