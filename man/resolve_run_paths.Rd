% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-paths.R
\name{resolve_run_paths}
\alias{resolve_run_paths}
\alias{print.cactus_run_paths}
\title{Resolve Every File Path of a Phylogenetic Run from Its Naming Convention}
\usage{
resolve_run_paths(
  output_dir = "7_Phylogenetics",
  prefix = "cactus",
  supermatrix = file.path("6_Concatenated", "concatenated_alignments",
    "ALIGNMENT_supermatrix.phy"),
  require = NULL
)

\method{print}{cactus_run_paths}(x, ...)
}
\arguments{
\item{output_dir}{Character. Run directory holding the inference outputs. Defaults to \code{"7_Phylogenetics"}.}

\item{prefix}{Character. Run prefix shared by every file of the run. Defaults to \code{"cactus"}.}

\item{supermatrix}{Character. Path to the concatenated supermatrix exported by
\code{\link[=run_concatenation_pipeline]{run_concatenation_pipeline()}}. Defaults to the Module 6 location.}

\item{require}{Character vector. Names of entries that must already exist on disk. Any that do
not trigger an error naming the module responsible for writing them. Defaults to \code{NULL}.}

\item{x}{A \code{cactus_run_paths} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{cactus_run_paths}: a named list of absolute paths, plus \code{was_reduced},
\code{ml_on_cluster}, and an \code{exists} list of logicals reporting which files are present.
}
\description{
Reconstructs the absolute path of every file a \code{PhyloCactus} inference run produces, given only
the output directory, the run prefix and the supermatrix location. Because each path is derived
rather than carried in memory, any stage of the pipeline can be executed in a fresh R session
without first re-running the stages before it.
}
\details{
The pipeline is a sequence of stages whose outputs feed the next, and a linear script holds those
outputs in variables such as \code{analysed_phy} or \code{ml_results}. Restarting R and resuming at a later
stage clears those variables, and the stage fails on a missing object rather than on a missing
file. Hard-coding the filenames instead removes the dependency on session state but reintroduces
the problem this naming convention exists to solve: the same literal name is retyped at a dozen
call sites, drifts out of step with the run prefix, and continues to be read after it has stopped
describing its contents.

Deriving the paths from the convention keeps a single definition of every filename while leaving
each stage independently runnable. Two entries are resolved by inspection rather than by
convention alone, because they record facts about the run rather than choices about naming:

\itemize{
\item \code{analysed_phy} is the reduced PHYLIP when \code{RAxML-NG} collapsed identical terminals during
validation, and the supermatrix itself when it did not. \code{was_reduced} reports which occurred.
\item \code{best_tree} and \code{ml_trees} resolve to the \verb{ml_search/} subdirectory when the maximum
likelihood search was submitted to a cluster, and to the run root when it was executed locally.
}

Passing \code{require} converts a missing input into an error naming the stage that produces it,
instead of letting the failure surface several calls later as an unreadable file.
}
\examples{
\dontrun{
# Start of any stage, in a fresh R session
paths <- resolve_run_paths(output_dir = "7_Phylogenetics", prefix = "cactus")
paths

# Refuse to start Module 9 unless Module 8 actually finished
paths <- resolve_run_paths(require = c("best_tree", "constraint_tree"))
}
}
\seealso{
\code{\link[=preprocess_partitions]{preprocess_partitions()}}, \code{\link[=calculate_ml_tree]{calculate_ml_tree()}}, \code{\link[=collect_bootstraps]{collect_bootstraps()}}.
}
