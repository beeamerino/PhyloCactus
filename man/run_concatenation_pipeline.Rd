% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concatenation.R
\name{run_concatenation_pipeline}
\alias{run_concatenation_pipeline}
\title{Concatenate Locus Alignments and Build Partition Coordinate Maps}
\usage{
run_concatenation_pipeline(
  input_dir,
  output_dir,
  outgroup_pattern = NULL,
  min_coverage = 0.2,
  exclude_markers = NULL
)
}
\arguments{
\item{input_dir}{Character. Path to directory containing curated, aligned locus FASTA files.}

\item{output_dir}{Character. Path to destination root directory for concatenated alignments and partition maps.}

\item{outgroup_pattern}{Character, named character vector, or \code{NULL}. Regular expression identifying outgroup terminals, or a named character vector of expressions (e.g. \code{c(Anacampserotaceae = "...", Portulacaceae = "...", Talinaceae = "...")}) to additionally compute per-group coverage columns. When supplied, \code{\link[=report_marker_group_coverage]{report_marker_group_coverage()}} runs before concatenation and its table is written to \code{logs_and_qc/SUPP_TABLE_marker_group_coverage.csv}. Defaults to \code{NULL}.}

\item{min_coverage}{Numeric. Fraction of non-gap, non-missing characters at which a terminal counts as covered by a marker, passed to \code{\link[=report_marker_group_coverage]{report_marker_group_coverage()}}. Defaults to \code{0.2}.}

\item{exclude_markers}{Character vector or \code{NULL}. Markers to leave out of the supermatrix, named
as they appear in the alignment file names without the \code{Masked_} prefix and without the
extension. The files are not modified or removed, so an excluded locus remains available for
other analyses; only this supermatrix is built without it. The excluded markers are named in
the run log and listed in \code{logs_and_qc/TABLE_markers_excluded.csv}. A name that matches no
alignment raises a warning rather than failing, so a typo is visible instead of silent.
Defaults to \code{NULL}.}
}
\value{
A data frame containing supermatrix dimensions, taxon coverage, and locus partition bounds.
}
\description{
Concatenates individual orthologous locus alignments end-to-end into a unified multilocus supermatrix.
Combining independent molecular loci increases statistical power to resolve difficult ancestral nodes while
allowing partitioned substitution modeling to account for mutational rate heterogeneity across molecular locus alignments.
Exports one coordinate map per partition format rather than one per downstream program: a
RAxML-style map (\code{PARTITION_raxml_style.txt}) read by \code{RAxML-NG}, \code{ModelTest-NG} and \code{IQ-TREE},
and a NEXUS SETS block (\code{PARTITION_nexus_charset.nex}). Configuration scripts for
\code{PartitionFinder2} and \code{MrBayes} are exported separately under their program names.
}
\examples{
\dontrun{
run_concatenation_pipeline(
  input_dir = "5_MAFFT_Cleaned/aligned_markers",
  output_dir = "6_Concatenated"
)

# A dating matrix without the nuclear locus that carries no outgroup coverage.
run_concatenation_pipeline(
  input_dir = "5_MAFFT_Cleaned/aligned_markers",
  output_dir = "6_Concatenated_dating",
  exclude_markers = "phyC"
)
}
}
