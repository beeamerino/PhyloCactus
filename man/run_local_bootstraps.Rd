% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{run_local_bootstraps}
\alias{run_local_bootstraps}
\title{Generate Non-Parametric Bootstrap Trees Locally}
\usage{
run_local_bootstraps(
  raxml_bin_path,
  aln_file,
  part_file = NULL,
  constraint_file,
  bs_trees = 500,
  outgroup = NULL,
  seed = NULL,
  threads = 8,
  workers = NULL,
  threads_per_worker = 4L,
  output_dir = dirname(aln_file),
  prefix = "cactus_bs"
)
}
\arguments{
\item{raxml_bin_path}{Character. System command or full path to executable \code{RAxML-NG} binary.}

\item{aln_file}{Character. Path to input PHYLIP supermatrix alignment file or pre-parsed \code{.rba} binary file.}

\item{part_file}{Character. Path to partition file specifying substitution models. Optional if \code{aln_file} is an \code{.rba} binary file. Defaults to \code{NULL}.}

\item{constraint_file}{Character. Path to Newick topological constraint scaffold file.}

\item{bs_trees}{Integer. Total number of non-parametric bootstrap trees to generate. Defaults to \code{500}.}

\item{outgroup}{Character vector of terminals passed to \code{RAxML-NG --outgroup}, or \code{NULL} (default); multiple terminals are joined with commas. \code{RAxML-NG} writes an unrooted topology with these terminals placed first, so this argument orders the output rather than rooting the tree: the root is imposed downstream by \code{automate_treePL()} via \code{ape::root(..., resolve.root = TRUE)}. Declaring the same set at every stage keeps the output ordering consistent across the maximum-likelihood search, the bootstrap replicates and the temporal bootstraps. Derive it with \code{resolve_rooting_outgroup()} rather than naming a terminal by hand.}

\item{seed}{Integer. Random seed for reproducible bootstrap initialization. Defaults to \code{NULL} (random).}

\item{threads}{Integer. Number of CPU threads. Defaults to \code{8} (or detected P-cores).}

\item{workers}{Integer. Parallel worker process count. If \code{NULL} (default), automatically calculated as \code{max(1L, as.integer(threads / threads_per_worker))}.}

\item{threads_per_worker}{Integer. Thread-to-worker ratio used to derive \code{workers} when \code{workers = NULL}. Defaults to \code{4L}
(unvalidated heuristic for local bootstrap workloads; see Details). Ignored if \code{workers} is set explicitly.}

\item{output_dir}{Character. Output directory for generated bootstrap trees. Defaults to \code{dirname(aln_file)}.}

\item{prefix}{Character. Output file prefix. Defaults to \code{"cactus_bs"}.}
}
\value{
Character path to the output bootstrap trees file (\code{.raxml.bootstraps}).
}
\description{
Performs non-parametric bootstrap resampling over supermatrix site columns to infer a distribution of bootstrap tree topologies (\code{RAxML-NG}).
Evaluates topological variation under non-parametric resampling to quantify node support via Felsenstein Bootstrap Proportions (FBP) or Transfer Bootstrap Expectation (TBE).
Optimizes multi-threading for local workstations (e.g., Apple Silicon) by allocating threads to Performance cores (P-cores)
and configuring parallel workers according to a configurable thread-to-worker ratio, avoiding thread contention with
Efficiency cores (E-cores).
}
\details{
Unlike \code{generate_bootstrap_script()}'s HPC-oriented default (empirically calibrated on a 128-core AMD EPYC node),
the \code{threads_per_worker = 4L} default here has not been empirically validated for bootstrap workloads on Apple
Silicon specifically (only single-threaded, non-coarse-grained ML tree search timings are currently available for
that hardware). Treat this default as a reasonable starting heuristic and re-validate empirically (e.g., time a
short run with \code{bs_trees} set low, at a couple of \code{threads_per_worker} values) before committing to a long local run.
}
\references{
Kozlov, A. M., Darriba, D., Flouri, T., Morel, B., & Stamatakis, A. (2019). RAxML-NG: a fast, scalable and
user-friendly tool for maximum likelihood phylogenetic inference. \emph{Bioinformatics}, 35(21), 4453-4455.
\doi{10.1093/bioinformatics/btz305}
}
