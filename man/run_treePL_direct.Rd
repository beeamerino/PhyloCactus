% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dating.R
\name{run_treePL_direct}
\alias{run_treePL_direct}
\title{Run treePL Executable Directly}
\usage{
run_treePL_direct(cfg_file, label, cwd = NULL)
}
\arguments{
\item{cfg_file}{Character. Path to \code{treePL} configuration file.}

\item{label}{Character. Descriptive run label identifier.}

\item{cwd}{Character. Optional working directory context. Defaults to \code{NULL}.}
}
\value{
Invisible NULL upon system command execution.
}
\description{
Executes the \code{treePL} binary directly to estimate divergence times from a prepared configuration file.
}
\details{
The tree path is read from the \code{treefile} line of \code{cfg_file} and checked for rootedness before
the binary is invoked, because \code{treePL} fails opaquely on an unrooted tree. When the
configuration declares no \code{treefile}, or the declared path cannot be resolved, the check is
skipped with a warning rather than blocking the run.
}
\references{
Smith, S. A., & O’Meara, B. C. (2012). treePL: divergence time estimation using penalized likelihood
for large phylogenies. \emph{Bioinformatics}, 28(20), 2689-2690. \doi{10.1093/bioinformatics/bts492}
}
