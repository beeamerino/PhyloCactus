% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/notify.R
\name{send_run_notification}
\alias{send_run_notification}
\title{Send a Run Notification by Email}
\usage{
send_run_notification(
  subject,
  body,
  to = NULL,
  credentials = NULL,
  enabled = TRUE
)
}
\arguments{
\item{subject}{Character. Subject line.}

\item{body}{Character. Message body, interpreted as Markdown by \code{blastula::md()}.}

\item{to}{Character or \code{NULL}. Recipient, also used as the sender. Defaults to \code{NULL}, which
reads the \code{MY_EMAIL} environment variable, normally set in \verb{~/.Renviron}.}

\item{credentials}{Character or \code{NULL}. Path to a \code{blastula} credentials file. Defaults to
\code{NULL}, which reads the \code{PHYLOCACTUS_SMTP_CREDS} environment variable and falls back to
\verb{~/.blastula_gmail}.}

\item{enabled}{Logical. \code{FALSE} returns immediately and sends nothing, so that a script can carry
the call unconditionally and switch it from one place. Defaults to \code{TRUE}.}
}
\value{
Invisibly \code{TRUE} when the message was handed to the SMTP server, \code{FALSE} otherwise.
}
\description{
Sends a short message when a long analysis ends, so that a run measured in hours does not have to
be watched. Intended for the steps this package runs on a local machine rather than on a cluster,
where the scheduler already mails its own notifications.
}
\details{
\strong{This function never fails a run.} Every reason it might not send, a missing address, a missing
package, a missing credentials file, an SMTP error, is reported with a \code{message()} and returns
\code{FALSE}. An analysis that took sixteen hours must not be lost because a mail server refused a
connection at the end of it.

\strong{Credentials are never read by this package.} The path is handed to \code{blastula::creds_file()},
which opens it. Nothing from that file is read, printed, logged or written anywhere by
\code{PhyloCactus}, and the file belongs outside the repository. Create it once with
\code{blastula::create_smtp_creds_file()}, and for a Gmail account use an application password rather
than the account password.
}
\examples{
\dontrun{
send_run_notification(
  subject = "PhyloCactus finished: treePL",
  body = "The dating run finished. Outputs are in 8_Dating."
)
}
}
