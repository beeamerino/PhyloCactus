% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barcoding_threshold.R
\name{sweep_barcoding_threshold}
\alias{sweep_barcoding_threshold}
\title{Sweep the Threshold of Remoteness of the Molecular Diagnostic Branch}
\usage{
sweep_barcoding_threshold(
  classifier_dir = file.path("11_barcoding", "7_classifier"),
  controls_dir = file.path("11_barcoding", "8_controls"),
  output_dir = file.path("11_barcoding", "9_threshold"),
  alignment = c("add", "library"),
  q = 0.99,
  quantile_type = 1L,
  figures = TRUE
)
}
\arguments{
\item{classifier_dir}{Character. Output directory of \code{\link[=classify_barcoding_folds]{classify_barcoding_folds()}}.}

\item{controls_dir}{Character. Output directory of \code{\link[=run_barcoding_controls]{run_barcoding_controls()}}. When it holds no
\code{TABLE_barcoding_cn2_queries.csv} the rejection is reported as not measured everywhere.}

\item{output_dir}{Character. Destination; refused inside a directory of the phylogeny.}

\item{alignment}{Character. Which predictions of step 7 are swept: \code{"add"}, the default, reads
the tables of \code{alignment = "add"}, measured the way a user query is measured; \code{"library"} reads
those of the joint alignment.}

\item{q}{Numeric. Quantile of the scheme G distances that sets the operating threshold. Defaults
to \code{0.99}.}

\item{quantile_type}{Integer. Type of \code{\link[stats:quantile]{stats::quantile()}}. Defaults to \code{1L}, whose result is always
a distance observed in the data.}

\item{figures}{Logical. Write one figure per locus, PDF and PNG at 300 dpi, 180 mm wide.}
}
\value{
Invisibly, a list with \code{curve} and \code{operating}. Writes
\code{TABLE_barcoding_threshold_curve.csv}, \code{TABLE_barcoding_threshold_operating.csv} and, with
\code{figures = TRUE}, \verb{FIG_barcoding_threshold_<locus>.pdf} and \code{.png}.
}
\description{
Step 9 of the branch. Without a rule of remoteness the nearest neighbour names a cactus species
for any query: in the negative control CN2, 288 of 312 outgroup queries came out in state 1. This
step applies the rule (a query whose distance to its nearest neighbour exceeds a threshold is
state 3, reason \code{remoteness}) over every threshold observed, and fixes an operating threshold per
locus.
}
\details{
For every locus and scheme the curve reports the legitimate queries in five disjoint outcomes
(correct species, wrong species, correct genus, wrong genus, not assigned; a wrong species and an
honest genus are never summed) and, when CN2 has comparable outgroup queries for the locus, how
many of them the rule rejects. Outgroup queries that match the locus in neither direction are not
comparable and stay out.

The operating threshold of a locus is the quantile \code{q} (type \code{quantile_type} of
\code{\link[stats:quantile]{stats::quantile()}}) of the distances of the legitimate queries of scheme G: a cactus whose
species is missing from the library while its genus is present, which is the query the rule must
not reject. It is fixed without the outgroup queries, which then report the rejection with its
exact binomial interval; a locus with no comparable outgroup query reports it as not measured.
}
\examples{
\dontrun{
sweep_barcoding_threshold(
  classifier_dir = "11_barcoding/7_classifier",
  controls_dir = "11_barcoding/8_controls",
  output_dir = "11_barcoding/9_threshold"
)
}
}
